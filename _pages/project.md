---
layout: single
permalink: /projects/
date: 2023-02-01
title: "수행한 모든 프로젝트 목록"
description: ""
subject: blog
toc_label: "All Projects"
toc_icon: "laptop-code"
toc_sticky: true
---

## Career

참고 목적으로 작성한 수행했던 프로젝트 이력입니다.

|  Company     | Working Date      | Period    |
|:------------:|:-----------------:|:---------:|
| 에이치디정션 | 2021.07 ~ 현재    | 재직중    |
| 스페이스컬쳐 | 2019.09 ~ 2020.01 | 5개월     |
| ADOP         | 2018.09 ~ 2019.09 | 1년       |
| 브이스테이션 | 2018.05 ~ 2018.08 | 3개월     |
| 미림미디어랩 | 2017.04 ~ 2018.01 | 10개월    |
| 진코퍼레이션 | 2015.02 ~ 2016.07 | 1년 6개월 |

## 에이치디정션


**EMR 서비스 운영**
* 서버 데드락 장애가 발생하는 화면에서 요청되는 모든 API를 브라우저 및 서버 로그를 통해 확인, QueryDSL을 사용하는 API가 동시에 호출되어 Qclass 초기화 시 순환 참조로 발생하는 것을 확인하여 서버 로딩 시 패키지 스캔을 통해 싱글스레드에서 초기화처리하여 해결
* 두개의 파드 중 하나가 연결이 안되는 상황에서, 나머지 파드로 트래픽이 몰렸을 때 나머지 파드가 OOMKilled(137) 상태로 죽는 것을 확인, 파드 yaml 및 스프링 부트 메모리 설정 등을 비교하여 스프링 부트 메모리 설정이 더 높다는 것을 확인하여 파드 메모리 설정을 증가시켜 처리
* ArgoCD를 통해 각 운영 서비스 릴리스 시 배포

**스테이지 서버 관리** (2022.11 ~ 2022.12) 1개월
* `Xen citrix`를 사용해 온프레미스 서버의 프로덕션과 동일한 노드 대수를 기준으로 스테이지 서버 구축 후  
  K8s(kubeadm, kubelet, kubectl 등) 설치 및 cgroup 설정 후에 사용중인 서비스 ArgoCD를 사용해 배포
* 스테이지 서버의 자원이 한정적이기 때문에 향후 파드가 자원 증대 요청시 거절되어 발생하는 장애를 방지하기 위해  
  파드 메모리 설정 중 limit, request를 동일한 값을 설정

**소규모 데이터 ETL 파이프라인 개발** (2021.10 ~ 2022.10) 1년

운영중인 EMR 서비스에서 생성되는 데이터를 FHIR(의료 데이터 국제 기술 표준)를 기준 포맷으로  
마이그레이션하여 네이버 측에 전송하는 데이터 ETL(Extract-Transform-Load) 파이프라인 구축

* 네이버와 협의 끝에 마이크로배치 정도 형태로 결정과 전송 필수를 위해 카프카를 도입, 기존 서비스 모듈에 JPA @EntityListener를 사용하여 데이터 입출력 시 카프카 이벤트 생성 기능 추가, 최신 데이터 송신을 위해 제로페이로드 포맷을 택해 최신데이터 상태를 컨슈머에게 위임
* 기존 서비스 멀티 모듈구조에서 JPA 엔티티 모듈을 의존하는 다른 모듈이 카프카 사용이 필요 없기 때문에 생성된 엔티티 모듈의 카프카 의존관계를 없애기 위해 인터페이스를 추가, 해당 기능을 처리하는 서비스 모듈에서 해당 인터페이스를 구현하는 방식으로 모듈 구조 리팩터링
* 카프카 브로커의 저장된 데이터를 읽어 조회, 및 FHIR 프레임워크를 활용해 포맷을 변경하여 네이버 FHIR 서버로 전송하는 이벤트 컨슈머 모듈 구현
* 약 1만건에 기존 데이터를 일괄 전송하는 API 구현하는 중에 너무 많은 데이터로 인해  
  발생하는 DB 커넥션 타임아웃을 컬렉션의 parallel과 스프링 @Async 조합으로 병렬 처리로 해결
* 데이터 송수신 시 보안을 위한 암복호화 FHIR 인터셉터 구현
* 컨슈머 모듈 배포를 위해 Jenkins 빌드 파이프라인, ArgoCD 설정

**EMR 서버 API 개발** (2021.07 ~ 2021.09) 3개월
* 정신과 진료 상담 시간을 측정하는 타이머 기능 개발
* 진단 및 처방 코드를 조합해서 사용하는 기능인 코드 묶음 기능 개발

---

## 스페이스컬쳐

### 인테리어 가구 추천 서비스의 API 서버 구축, 개발
(2019.09 ~ 2020.01) 5개월

**업무**
* Bootpay API를 활용하여 결제 및 검증, 취소 API 개발
* FireStore(구글 실시간 DB)에 채팅 데이터 보관 처리 구현
* 그 외 카트, 상품, 주문 등 주요 기능 CRUD API 개발 및 단위 테스트 작성 및 리팩터링
* AWS 3Tier(ELB-EC2-RDS) 인프라 구축 및 비용 관리
* Spring Boot Log 롤링 파일 설정 및 데이터소스 설정  
  결제 등 주요 API 별 롤링 파일 설정 및 서버 내 크론탭을 사용하여 삭제 처리

**사용 기술**
* Java(Spring Boot), MyBatis, Gradle, JUnit, AWS(EC2, RDS-Aurora), Bootpay API, FireStore

---

## 애드오피

### 인사 관리 시스템(Portal) 유지보수
(2019.08 ~ 2019.08) 1개월

**업무**
* AWS S3 SDK를 활용하여 전자 결재 시 영수증 파일 업로드 기능 수정
* 직원별 연차에 따라 휴가 일수를 다르게 처리하는 기능 버그 수정

**사용 기술**
* PHP(CI), JavaScript(jQuery), AWS(EC2, RDS-Aurora, S3)

---

### 광고 수익 정산 서비스(Insight) 유지보수
(2019.04 ~ 2019.07) 4개월

**업무**
* 국가별 환율에 따라 부가세 계산, 매체별 배분율 계산 등 수익 정산 기능 유지보수
* 정산 이월, 지급일 변경, 메일 전송 등 기능 유지보수
* 광고 수익 데이터 파일 업로드 기능 구현
* 기능별 중복 코드 리팩터링을 통해 구조 개선 및 쿼리 튜닝

**사용 기술**
* Java(Spring Boot), MyBatis, Gradle, JavaScript(jQuery), AWS(EC2, RDS-Aurora, ACM), Bitbucket, Jenkins

---

### AWS 비용 관리 및 리소스 상태 모니터링, 장애 대응
(2019.02 ~ 2019.03) 2개월

**업무**
* AWS RDS DB 서버의 인스턴스 스케일 업 (r4 > r5), S3 백업 기능 설정 추가
* EC2 스케일 아웃 시 AZ 내 가용 리소스가 부족한 상황 대응  
  Auto Scaling 그룹 설정의 다른 AZ 추가 및 VM 노드 직접 삽입하여 처리
* 슬로우 쿼리 확인 및 처리 (log_queries_not_using_indexes 옵션 활용)  
  slow_query_log 설정으로는 진짜 느리지 않은 쿼리까지 기록에 남아 위 옵션 활용
* 태그나 대시보드를 기준으로 사용되지 않는 서비스 모듈 및 AWS 리소스 정리  
  또는 사용하는 인스턴스 유형 변경
* 메가존 빌링 서비스를 활용해 월별 지출 비용 관리
* 비용 절감 목적으로 SpotInst(베스핀 기업의 스팟인스턴스 관리 서비스) 테스트 진행  
  서비스가 불안정하여 적용 실패

**사용 기술**
* EC2(AMI, ELB, Auto Scaling), RDS(Aurora), CloudWatch, CloudFront, IAM

**문제 해결**
* 트래픽이 평시와 비슷한 상태에서 DB CPU 사용량이 불특정한 주기로 급증하는 상태 확인
* AWS RDS 클러스터 파라미터 그룹의 slow_query_log 설정하면 슬로우 쿼리를 모두 볼 수 있음
  * 진짜 슬로우 쿼리가 아니더라도 해당 시점에 쿼리 실행 시간이 오래 소요되면 기록 됨
  * long_query_time으로 슬로우 쿼리 시간 설정 가능하나 시간만으로는 정확한 측정이 어려움
* 따라서 슬로우 쿼리 로그에 기록된 쿼리를 모두 실행 계획 확인
  * 실행 계획이 풀 테이블 스캔인 슬로우 쿼리를 찾아 해당 테이블에 인덱스 생성하여 처리
* 단순히 인덱스를 사용하지 않는 쿼리는 log_queries_not_using_indexes 옵션으로 확인 가능

---

### AMP 변환 서비스 개발 및 서버 유지보수
(2018.09 ~ 2019.01) 5개월

AMP(Accelerated Mobile Pages)는 일반 페이지보다 빠른 속도로 렌더링 되는 페이지

**업무**
* Jsoup를 사용하여 고객사(매체)가 제공하는 RSS를 AMP 데이터로 변환하는 크롤링 모듈 개발  
  리눅스 서버 크론탭으로 해당 모듈을 반복 실행 처리  
  Bitbucket(web hook)과 Jenkins(execute shell)를 사용하여 배포 자동화
* AMP 퍼블리싱 서버 매체별 규격에 맞춰 AMP 유효성 확인, AMP(+AMP Story) 템플릿 수정  
  구글 SEO(Search Engine Optimization) 가이드 적용, 문서화
* 구현한 스크래핑 모듈과 비슷한 모듈 6개를 모아 멀티 모듈로 구성하면서 관리 포인트 개선  
  이때 약 3개의 EC2 인스턴스 삭제를 통해 월 기준 약 $200 절약

**사용 기술**
* Java, Jsoup, MyBatis, Gradle, JavaScript(Vue.js), PHP(CI), AWS(EC2, RDS-Aurora, CloudFront), Bitbucket, Jenkins

**배운 점**
* 멀티 모듈 구성시에는 하위 모듈에서 영향을 받기 때문에 상위 모듈은 변화가 적고 안정적이게 구성할 것

---

## 미림미디어랩

### 콘텐츠 동영상 재생 서비스 개발
(2017.04 ~ 2018.01) 10개월

한국방송통신전파진흥원 지원사업 방송 콘텐츠 부분 최우수상 수상

**업무**
* Ionic 프레임워크를 활용해 하이브리드 앱 개발 및 화면(렌더링) 구현
* 채널, 시청 기록, 구독, 코멘트 등 서버 API 개발
* Vimeo(동영상 파일 서버) API 호출 제한 횟수 초과 장애 처리  
  ehCache(캐시 라이브러리)를 사용하여 영상 데이터 캐싱 처리
* Spring Boot Log 롤링 파일 설정 및 데이터소스 설정

**사용 기술**
* Java(Spring Boot), MyBatis, Gradle, TypeScript(Ionic), AWS(EC2, RDS-PostgreSQL), Vimeo(동영상 파일 서버) API

**배운 점**
* 정적 데이터를 서빙하는 것은 로컬 캐시보다 CDN 등 캐시 서버의 캐싱하는 것이 더 효율적
  * 로컬 캐시로는 서버로 유입되는 트래픽 자체를 줄일 수 없음
  * 또한 용량이 큰 데이터를 서버에서 직접 캐싱 및 서빙하는 것에 대한 오버헤드 존재

---

## 진코퍼레이션

### 아모레퍼시픽 오산 물류센터 B2B 관리 시스템 유지보수
(2016.06 ~ 2016.07) 2개월

**업무**
* 엑셀파일 업로드 기능 구현, 데이터 운송장 출력 기능 구현

**사용 기술**
* PowerBuilder, Oracle

---

### 아모레퍼시픽 김천 물류센터 B2C 검품 시스템 개발
(2015.12 ~ 2016.05) 6개월

**업무**
* RS232, RS485 시리얼 통신으로 저울, 스캐너, 프린터 등 장비 통신 기능 구현
* 피킹 제품 일치 여부 확인, 배송 정보 조회, 운송장 출력 등 기능 구현
* 현장 통합 테스트 진행을 위해 경북 김천에 6개월 간 상주

**사용 기술**
* VB6, Oracle, MS Access

---

### 아모레퍼시픽 오산 물류센터 PCS(Picking Cart System) 유지보수
(2015.08 ~ 2015.11) 4개월

**업무**
* 피킹 프로세스 변경 및 운송장, 주문 내역서 등 출력 데이터, 포맷 변경
* 제품과 랙 위치 레이아웃 등 화면 변경

**사용 기술**
* VB6, Oracle, MS Access

---

### 아모레퍼시픽 TMS 배송 관리 앱 유지보수
(2015.07 ~ 2015.07) 1개월

**업무**
* 앱 문자 송신 기능 버그 수정, 메시지 내용 변경

**사용 기술**
* Java(Android)

---

### 아모레퍼시픽 미국 물류센터 B2B 검품, PCS 개발
(2015.03 ~ 2016.06) 4개월

**업무**
* 검품 프로그램의 피킹 제품 일치 여부 확인, 배송 정보 조회, 운송장 출력 등 기능 구현
* PCS의 제품 조회, 피킹 처리 기능 수정
* 현장 통합 테스트 진행을 위해 미국 LA에 1개월 간 상주

**사용 기술**
* VB6, C#, Oracle, MS Access

---

### 아모레퍼시픽 BMS(Buy-Move-Sell) 자재 입출고 관리 시스템 개선
(2015.02 ~ 2015.02) 1개월

**업무**
* 자재 조회 및 입출고 기능 개발

**사용 기술**
* Java(Spring-JSP), Oracle
